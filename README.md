# Demo PR Repository

This repository is used as a controlled demo target for AI PR Review Assistant.

The `main` branch intentionally stays minimal. Demo risk examples should live in
feature branches so the review assistant can analyze a clear Pull Request diff.
